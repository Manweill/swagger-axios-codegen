// import { DeviceService } from './output/api'

// const deviceService = new DeviceService()
// async function name(params) {
//   const result = await deviceService.getDevices({ skipCount: 0, maxResultCount: 100, deviceAreaId: 0 })
// }
